# 08 - Comments: list, create, get, delete

## References
- OpenAPI spec: `docs/openapi.json` (endpoints: /v1/comments/*)
- Live docs: https://developers.notion.com/llms.txt

## Overview
- Implement `notion-cli comment list --block <block_id>` - list comments on a block/page
- Implement `notion-cli comment create` - add a comment
- Implement `notion-cli comment get <comment_id>` - get a comment
- Implement `notion-cli comment delete <comment_id>` - delete a comment

## Context
- Depends on plans 01 (foundation), 02 (API client), 03 (output)
- Comments are attached to blocks or pages via `block_id` query param
- Comment object has: id, parent, discussion_id, rich_text, created_time, created_by
- List endpoint is paginated

## Development Approach
- **Testing approach**: TDD (tests first, then implementation)
- **CRITICAL: every task MUST include tests written BEFORE implementation code**
- **CRITICAL: all tests must pass before starting next task**

## Implementation Steps

### Task 1: Comment types and list API method
- [x] write tests in `internal/api/comments_test.go`:
  - ListComments returns paginated comments for a block
  - comment JSON deserialization
  - error handling
- [x] define Comment struct in `internal/api/comments.go` matching openapi.json schema
- [x] implement ListComments method on Client (GET /v1/comments?block_id=...)
- [x] run tests - must pass before next task

### Task 2: `comment list` command
- [x] write tests in `internal/cli/comment_test.go`:
  - outputs comments for a block
  - error for missing --block flag
  - handles pagination
  - respects output format
- [x] implement `comment list` cobra subcommand in `internal/cli/comment.go`
- [x] run tests - must pass before next task

### Task 3: Create comment API method and command
- [x] write tests for CreateComment:
  - creates comment on a page (parent: { page_id })
  - creates comment in a discussion (discussion_id)
  - sends rich_text content
  - error handling
- [x] implement CreateComment method on Client (POST /v1/comments)
- [x] write tests for `comment create` command with --page/--discussion and --text flags
- [x] implement `comment create` cobra subcommand
- [x] run tests - must pass before next task

### Task 4: Get and delete comment API methods and commands
- [x] write tests for GetComment and DeleteComment:
  - get returns comment by ID
  - delete removes comment
  - error handling for both
- [x] implement GetComment (GET /v1/comments/{id}) and DeleteComment (DELETE /v1/comments/{id})
- [x] write tests for `comment get` and `comment delete` commands
- [x] implement both cobra subcommands
- [x] run tests - must pass before next task

### Task 5: Verify acceptance criteria
- [ ] verify all comment commands work correctly
- [ ] verify pagination in list
- [ ] verify rich_text creation
- [ ] run full test suite with `make test`
- [ ] run linter with `make build` - all issues must be fixed

## Technical Details
- Comment struct: id, object, parent, discussion_id, rich_text, created_time, created_by, last_edited_time
- GET /v1/comments?block_id={id} - paginated
- POST /v1/comments - body: { parent: { page_id } | discussion_id, rich_text: [...] }
- GET /v1/comments/{comment_id}
- DELETE /v1/comments/{comment_id}
- Rich text: array of { type: "text", text: { content: "..." } }

## Post-Completion
- Comments are standalone, no downstream dependencies
