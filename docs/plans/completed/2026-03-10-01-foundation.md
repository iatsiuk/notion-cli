# 01 - Foundation: Project Structure, CLI Skeleton, Configuration

## References
- OpenAPI spec: `docs/openapi.json`
- Live docs: https://developers.notion.com/llms.txt

## Overview
- Initialize Go module and project structure
- Create CLI entry point with cobra root command
- Implement configuration: token from flag/env, output format, quiet/verbose flags
- Define exit codes and error types
- Add `status` command (health check to Notion API)

## Context
- No Go code exists yet, only config files (Makefile, .golangci.yml, .goreleaser.yaml, CI)
- Makefile expects `cmd/notion-cli/main.go` as entry point
- README defines global flags: --token, --format, --quiet, --verbose
- README defines exit codes: 0 (success), 1 (connection), 2 (API), 3 (auth)

## Development Approach
- **Testing approach**: TDD (tests first, then implementation)
- Complete each task fully before moving to the next
- **CRITICAL: every task MUST include tests written BEFORE implementation code**
- **CRITICAL: all tests must pass before starting next task**
- Run `make build` to verify linting after each task

## Implementation Steps

### Task 1: Initialize Go module and project structure
- [x] run `go mod init notion-cli` and add cobra dependency
- [x] create directory structure: `cmd/notion-cli/`, `internal/config/`, `internal/cli/`
- [x] create minimal `cmd/notion-cli/main.go` with empty main()
- [x] verify `make build` succeeds

### Task 2: Define exit codes and error types
- [x] write tests for exit code constants and error type mapping in `internal/cli/exitcode_test.go`
- [x] implement exit codes (0, 1, 2, 3) and typed errors in `internal/cli/exitcode.go`
- [x] run tests - must pass before next task

### Task 3: Configuration loading (token, format, flags)
- [x] write tests for config loading in `internal/config/config_test.go`:
  - token from env var NOTION_TOKEN
  - token from flag overrides env var
  - format auto-detection (json for TTY, jsonl for pipe)
  - format flag override
  - quiet and verbose flags
  - error when no token provided
- [x] implement `internal/config/config.go` with Config struct and Load function
- [x] run tests - must pass before next task

### Task 4: Root cobra command with global flags
- [x] write tests for root command flag parsing in `internal/cli/root_test.go`
- [x] implement root command in `internal/cli/root.go` with --token, --format, --quiet, --verbose flags
- [x] wire config loading into PreRunE
- [x] run tests - must pass before next task

### Task 5: Wire main.go to root command
- [x] update `cmd/notion-cli/main.go` to execute root command with proper exit code handling
- [x] verify `make build` succeeds
- [x] verify `./notion-cli --help` shows expected flags
- [x] run all tests - must pass before next task

### Task 6: Add `status` command
- [x] write tests for status command in `internal/cli/status_test.go` (mock HTTP)
- [x] implement `notion-cli status` command that calls GET /v1/users/me as health check
- [x] handle connection error (exit 1), API error (exit 2), auth error (exit 3)
- [x] run tests - must pass before next task

### Task 7: Verify acceptance criteria
- [x] verify all global flags work: --token, --format, --quiet, --verbose
- [x] verify exit codes are correct for each error type
- [x] verify NOTION_TOKEN env var is respected
- [x] run full test suite with `make test`
- [x] run linter with `make build` - all issues must be fixed

## Technical Details
- Config struct: Token, Format (json|jsonl|raw|table|auto), Quiet, Verbose
- Format auto-detection: check os.Stdout with isatty
- Exit codes: 0=success, 1=connection, 2=API, 3=auth
- Status command: lightweight GET /v1/users/me, display ok/error

## Post-Completion
- Foundation is prerequisite for all subsequent plans
- API client (plan 02) will build on config and error types
