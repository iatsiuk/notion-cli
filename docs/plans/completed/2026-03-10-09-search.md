# 09 - Search: search pages and databases

## References
- OpenAPI spec: `docs/openapi.json` (endpoint: POST /v1/search)
- Live docs: https://developers.notion.com/llms.txt

## Overview
- Implement `notion-cli search <query>` - search pages and databases
- Support filtering by object type (page/database)
- Support sort by last_edited_time
- Handle paginated results

## Context
- Depends on plans 01 (foundation), 02 (API client), 03 (output)
- Search endpoint returns mixed results (pages and databases)
- Supports filter.value (page|database) and filter.property (object)
- Supports sort.direction (ascending|descending) and sort.timestamp (last_edited_time)

## Development Approach
- **Testing approach**: TDD (tests first, then implementation)
- **CRITICAL: every task MUST include tests written BEFORE implementation code**
- **CRITICAL: all tests must pass before starting next task**

## Implementation Steps

### Task 1: Search API method
- [x] write tests in `internal/api/search_test.go`:
  - search with query string
  - search with filter (pages only, databases only)
  - search with sort direction
  - pagination handling
  - empty results
  - error handling
- [x] implement Search method on Client (POST /v1/search)
- [x] run tests - must pass before next task

### Task 2: `search` command
- [x] write tests in `internal/cli/search_test.go`:
  - search with query argument
  - --type flag filters by page/database
  - --sort flag sets sort direction
  - handles pagination
  - respects output format
  - error for empty query
- [x] implement `search` cobra command in `internal/cli/search.go`
- [x] run tests - must pass before next task

### Task 3: Verify acceptance criteria
- [x] verify search returns pages and databases
- [x] verify type filter works
- [x] verify sort works
- [x] verify pagination
- [x] run full test suite with `make test`
- [x] run linter with `make build` - all issues must be fixed

## Technical Details
- POST /v1/search
- Body: { query: string, filter?: { value: "page"|"database", property: "object" }, sort?: { direction: "ascending"|"descending", timestamp: "last_edited_time" }, start_cursor?, page_size? }
- Response: paginated list of page and database objects

## Post-Completion
- Search is standalone, no downstream dependencies
